<?xml version="1.0" encoding="utf-8" ?>

<odoo>

    <record id="view_catalog_webhook_event_tree" model="ir.ui.view">
        <field name="model">catalog.webhook.event</field>
        <field name="arch" type="xml">
            <tree editable="bottom" decoration-muted="not active">
                <field name="active" invisible="1"/>
                <field name="name"/>
                <field name="code"/>
                <field name="event"/>
                <field name="description"/>
                <field name="service"/>
            </tree>
        </field>
    </record>

</odoo>
